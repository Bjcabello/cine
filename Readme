NODE.JS

Este proyecto permite:

- Crear ![alt text](img/image-1.png), leer ![alt text](img/image-2.png), actualizar ![alt text](img/image-3.png)y eliminar películas ![alt text](img/image-4.png) y salas de cine ![alt text](img/image-7.png).

Dependencias principales:

    express .
    pg
    cors
    dotenv
    morgan

🔧 Dependencias de desarrollo:

    typescript
    ts-node-dev
    @types/express
    @types/node
    @types/cors
    @types/swagger-jsdoc
    @types/swagger-ui-express
    @types/pg

ANGULAR.js

solo boootrap se instalo ya que es solo lectura no esta trayendo nada de la base de datos

- Interfaz en Angular para login ![alt text](img/image-1.png) , dashboard ![alt text](img/image-2.png) y el formulario de crear pelicula (solo lectura no injecta desde la base de datos).
